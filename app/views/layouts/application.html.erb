<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Spruceling" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Spruceling" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= cloudinary_js_config %>

    <meta property="og:title" content="Spruceling - Every kid outgrows their stuff. Why not swap it?"/>
    <meta property="og:type" content="company"/>
    <meta property="og:site_name" content="Spruceling"/>
    <meta property="og:url" content="http://www.spruceling.com"/>
    <meta property="og:image" content="null"/>
    <meta property="og:description" content=""/>

    <script type="text/javascript" src="//use.typekit.net/tfd2aze.js"></script>
    <script type="text/javascript">try{Typekit.load();}catch(e){}</script>

    <%= yield(:head) %>
  </head>
  <body id="full">
    <div id="container">

      <%= render 'layouts/navigation' %>

      <%= render 'layouts/messages' %>

      <div class="row">
        <%= yield :left_sidebar %>

        <div id="main" role="main" class="<%= content_for?(:left_sidebar) ? "nine" : "twelve" %> columns">
          <%= yield %>
        </div>
      </div>

      <div id='footer'>
        Footer
      </div>
    </div>

    <script type="text/javascript" src="https://js.stripe.com/v1/"></script>
    <script type="text/javascript">
      // this identifies your website in the createToken call below
      Stripe.setPublishableKey('<%= STRIPE_PUBLIC_KEY %>');
    </script>
  </body>
</html>