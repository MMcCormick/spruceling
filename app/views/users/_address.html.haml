%div= address["full_name"] if address["full_name"]
%div= address["address1"] if address["address1"]
%div= address["address2"] if address["address2"]
%div= "#{address["city"] if address["city"]}#{", #{address["state"]}" if address["state"]} #{address["zip_code"] if address["zip_code"]}"