%div{:class => "item_form_teaser item-#{item.id}"}
  %span.brand= item.brand.name
  %span.type= item.type_singular
  - if item.new_with_tags
    %span.nwt [new with tags]
  = link_to "X", item_path(item), :method => :delete, :remote => true, 'data-skip-pjax' => true, :class => 'delete'