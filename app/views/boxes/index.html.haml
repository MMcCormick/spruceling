- content_for :under_header do
  = render :partial => "charity/charity_meter"

- content_for :left_sidebar do
  = render :partial => "sidebar", :layout => 'layouts/left_sidebar'

.box_paginator.before
  = paginate @boxes

#box_list
  - @boxes.each do |box|
    = render 'boxes/teaser', :box => box

  -if @boxes.length != 12
    = render 'boxes/no_more_boxes'

%br

.box_paginator
  = paginate @boxes
